setup:
  config:
    GO_VERSION: 1.22
build:
  packages:
    - prisma-engines
  config:
    DEVELOPMENT: false
  pre:
    - go run github.com/steebchen/prisma-client-go generate
  golang:
    main: main.go
    binary: bin/backend
run:
  web: bin/backend
